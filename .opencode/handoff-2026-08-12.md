# Fork opencode — Handoff (2026-08-12)

Contexto para retomar el trabajo tras reiniciar este fork.

## Máquina
- RTX 3050 Mobile **4GB VRAM**, **24GB RAM** (8 soldada + 16 SODIMM), i7-11370H (8 threads).
- Repo fork: `/home/methodwhite/opencode` (workdir `packages/opencode`).

## 1. Fix de recursos de sistema (hecho)
- `swappiness` 150 → **10** (permanente: `/etc/sysctl.d/99-swappiness.conf`). Era la causa del cuelgue por swap thrashing.
- Procesos huérfanos eliminados: 2× `noctua_web_serve`+conmon+pasta (contenedores podman muertos), `dolphin` daemon, `at-spi`, `baloo` (disabled).
- `kdeconnectd` se mantiene (parte de KDE, no desactivar).

## 2. Worker llama.cpp — cambios en el fork (working tree, SIN commitear en el fork)
Archivos modificados en `packages/opencode/src`:
- `provider/llama.ts`: `DEFAULT_CONTEXT_LENGTH` 65536→16384; `buildLlamaArgs()` extraído (función pura exportada); kv-cache q8_0 por defecto (`--cache-type-k/v q8_0`); `-np` slots; `-t` threads; `--fit`/`--fit-target` respetados.
- `provider/provider.ts`: `contextLength` ahora cae a `model.limit.context` cuando no hay `contextLength` explícito (antes se ignoraba y el server lanzaba con -c 65536). Conecta `kvCacheQuantized`, `threads`, `slots` (default 1).
- `test/provider/llama.test.ts`: 9 tests para `buildLlamaArgs`.
- `test/mcp/oauth-provider.test.ts`: `determineScope` ya no existe en SDK MCP 1.29 → implementado localmente en el test (typecheck + 11 tests OK).

Typecheck: limpio salvo nada (el error preexistente de `determineScope` se arregló).

## 3. Config del proyecto fork (`.opencode/opencode.jsonc`)
- `qwen2.5-3b` `contextLength` 65536→32768.
- Qwen9B ya estaba bien: `contextLength 32768` + `fit:true` + `flashAttention:true` + `fitTargetMiB:512`.

## 4. Synapsis MCP (repo aparte, COMMITEADO y pusheado)
Repo: `/mnt/workspace/Proyectos/synapsis/synapsis`, branch `develop`.
- **`b8bec30`**: port de `standares-mcp` a Rust → `src/presentation/mcp/standares.rs`. Resources `standares://list`, `/standards/<cat>`, `/skills/...`; tools `standares_search/stats/list`; `resources/templates/list` implementado (arregla init del cliente). 905 resources. `STANDARES_ROOT` env override, default `~/Standares`, degrada a 3 resources si ausente.
- **`b5e205e`**: elimina rutas absolutas del usuario en scripts; installers resuelven última release vía GitHub API (antes 0.8.2 hardcodeado); configs usan `synapsis-mcp` por PATH.
- Binario release instalado en `~/.local/bin/synapsis-mcp` (0.12.0, 13.7MB).

## 5. Configs del sistema (actualizadas, todas por PATH)
- opencode: `mcp.synapsis.command = ["synapsis-mcp"]` (fork + main).
- gemini, mimocode, gh: corregidas de ruta absoluta rota a `synapsis-mcp` (backups `.bak` al lado).
- `standares` MCP server eliminado de configs opencode; script Python borrado (respaldo `/var/tmp/opencode-backup/standares-mcp.py.bak`).

## 6. Modos Compose + Auto — cambio inteligente de modo (working tree, SIN commitear)

Petición del usuario: faltaba el modo **Compose** y un modo **Auto** que cambie entre Plan/Compose/Build solo, sin preguntar (como Cursor).

Decisiones confirmadas con el usuario:
- **Auto = cambio real de agente** vía nueva tool `switch_mode` (no solo un prompt).
- **Compose = full edit + terminal**, con prompt de workflow (como plan pero con permisos de build).

### Cambios (todos en working tree, sin commitear)
- `packages/opencode/src/agent/agent.ts`: nuevos agentes primarios `compose` (color `accent`) y `auto` (color `primary`); permiso `switch_mode` (`deny` en defaults, `allow` en build/plan/compose/auto).
- `packages/opencode/src/tool/switch-mode.ts` (+ `switch-mode.txt`): tool `switch_mode` que crea mensaje de usuario sintético con el agente destino (`plan|compose|build`) — mismo mecanismo que `plan_exit` pero **sin pregunta**.
- `packages/opencode/src/tool/registry.ts`: `switch_mode` registrado para todos los clients (no gated por experimentalPlanMode).
- `packages/opencode/src/session/prompt/auto-mode.txt` y `compose-mode.txt`: prompts de workflow; inyectados en `session/reminders.ts` (agente `compose`/`auto`, dedupe por `hasReminder`).
- `plan-mode.txt` y `plan.txt`: fase 5 ahora permite handoff vía `switch_mode("build")` cuando plan entró automáticamente.
- `packages/tui/src/routes/session/index.tsx:333`: TUI escucha `switch_mode` completado → `local.agent.set(mode)`.
- `packages/opencode/src/cli/cmd/run.ts` + `run/tool.ts`: regla de render `switch_mode` (block) + deny en modo no interactivo.
- `packages/core/src/plugin/agent.ts` (V2) + `packages/core/src/v1/config/config.ts`: espejo de `compose`/`auto` y clave de config.

Typecheck limpio en `packages/opencode`, `packages/tui`, `packages/core`. Tests de permission OK.

### Cómo probar / usar
- Ciclar agentes con Tab hasta `auto` o `compose`, o `default_agent: "auto"` en config.
- Flujo Auto: `auto` evalúa → `switch_mode("plan")` → plan escribe plan → `switch_mode("build")` → build implementa, todo sin preguntar.
- El modo Auto queda como sesión activa para la siguiente entrada del usuario.

## Pendiente / siguiente
1. **Reiniciar opencode** (y gemini/mimocode/gh) para cargar binario release de synapsis-mcp.
2. **Probar los modos Compose/Auto en la TUI** (`tmux new-session -d -s opencode-dev 'bun dev'`) — falta validación en runtime; en especial: Auto→plan→build sin preguntas, y el handoff vía switch_mode.
3. Plan de entrenamiento Qwen9B:
   - Abliteración HSAQ en 4-bit (NF4, ~6GB) en vez de bf16 19.3GB (`abliterate_hsaq.py` en `/home/methodwhite/Proyectos/ai-ml/hacksec-finetune/scripts/`).
   - Fine-tune QLoRA 4-bit + paged_adamw_8bit + gradient checkpointing con offload CPU (4GB VRAM + 24GB RAM).
   - Usuario quiere HSAQ en la propia abliteración Y en el fine-tune (menos pérdida de inteligencia).
   - Dataset primero: ampliar `data/security_dataset.jsonl` (hacksec-finetune).
4. Modelo base `qwen3.5-9b-base` descarga incompleta: 11/15 archivos, sin safetensors shards (~5GB, faltan ~14GB). Retomar con `huggingface-cli`.
